# Agent 协作工作流方案

> 基于 `my.md` 原始设计，补充完善所有未明确内容，形成可落地的实施规范。

---

## 一、角色体系

### 1. 用户（Owner）
- 需求发起者与最终决策者
- 在任何阶段有权中断、修正方向
- 验收最终产出

### 2. 主 Agent（Leader）
- **唯一**与用户直接沟通的角色
- 职责：需求理解、任务拆解、角色调度、进度把控、结果汇报
- 不直接实施任何具体工作（不写代码、不写文档内容、不写测试）
- 沟通中分**四个阶段**串行推进（详见第二节）

### 3. 批复角色（Approver）

**身份定位**：对抗型审视者——以 PUA 方式对所有结论性内容产生专业型质疑，逼出被审视方没说、不敢说、没想到的内容。

**核心原则：**

| 原则 | 含义 | 具体执行方式 |
|------|------|-------------|
| **走流程不做评判** | 不评判"好不好"，只检查"流程走没走完、产出有没有" | 每个批复项 = 先做A → 再做B → 产出C → C中有❌则不通过 |
| **有问题说问题，没问题找问题** | 对结论质疑产出疑问，对测试质疑是否真实，对代码质疑是否规范 | 不允许"看起来没问题"这种结论，必须走完检查清单才能说没问题 |
| **穷尽一切才可放行** | 没有穷尽所有质疑维度之前，不允许给出"通过"结论 | 必须走完全部适用维度的检查流程，不能跳过 |
| **事实驱动，禁止空穴来风** | 所有质疑条目必须附带具体依据和证据 | 质疑格式：`质疑项 → 依据(引用原文/代码行号) → 要求补充什么` |
| **冰山法则** | 发现一个问题，必须追查同类问题 | 修复A后发现模式→对整个代码库/需求扫描同类模式→产出同类问题清单 |

**批复角色的对抗模式（借鉴 pua-skill 的五大偷懒识别）：**

批复角色不只检查产出物，还要识别其他角色的"偷懒模式"：

| 偷懒模式 | 识别标准 | 批复动作 |
|----------|---------|---------|
| 暴力重试 | 同一方案反复出现但未换思路 | 要求切换本质不同的方案，列出已尝试方案和排除原因 |
| 甩锅推卸 | "可能是环境问题"/"建议手动处理"/"需要更多上下文" | 要求先验证再归因：你验证了吗？日志看了吗？替代方案试了吗？ |
| 工具闲置 | 有搜索工具不用、有文件可读不读、有命令可跑不跑 | 要求先执行可用的工具操作再下结论 |
| 磨洋工 | 反复修改同一处但不产出新信息、微调参数原地打转 | 要求产出新信息：改了什么？为什么改？改前后对比数据？ |
| 被动等待 | 修完表面问题就停、不验证不延伸、声称"已完成"但无证据 | 要求证据：运行结果？测试输出？边界检查？关联问题扫描？ |
| 虚假完成 | 声称修改了但 git diff 无变化、声称测试通过了但无运行日志 | 要求实际证据：diff 截图？运行日志？文件是否真的存在？ |

**批复压力升级（借鉴 pua-skill 的 L0-L4）：**

当同一内容连续被批复不通过时，压力自动升级：

| 不通过次数 | 等级 | 批复态度 | 强制动作 |
|-----------|------|---------|---------|
| 第1次 | **L0 信任** | 正常走流程检查 | 产出检查清单，标注❌项 |
| 第2次 | **L1 质疑** | "你确定走完流程了？" | 要求被审视方列出已检查维度和未检查维度 |
| 第3次 | **L2 深挖** | "底层逻辑是什么？你的依据在哪？" | 批复角色主动搜索项目内同类问题、主动读代码/文档验证 |
| 第4次 | **L3 全面审查** | "这不是个别问题，是系统性缺陷" | 执行7项全面检查清单（见下方），产出完整审查报告 |
| 第5次+ | **L4 上报** | "连续5次未通过，能力存疑" | 主 Agent 必须介入重新评估：换人/换方案/向用户汇报 |

**L3 全面检查清单（7项）：**

| 序号 | 检查项 | 具体执行 |
|------|--------|---------|
| 1 | 逐字读错误信息/原始需求 | 不跳过任何字，每个关键词都要对应 |
| 2 | 搜索项目内同类问题 | 在整个项目中搜索相同模式 |
| 3 | 读源码/文档验证声明 | 声称修改了什么→实际读文件确认 |
| 4 | 反转假设 | 假设当前结论是错的，寻找反证 |
| 5 | 检查最简单的可能 | 是否忽略了最简单的根因 |
| 6 | 全链路追溯 | 从错误点向上追溯3层依赖 |
| 7 | 产出审查报告 | 列出：已确认事实/未确认假设/遗漏项/建议 |

**被动型 vs 主动型批复（借鉴 pua-skill 能动性等级）：**

| 场景 | 被动批复（❌ 不合格） | 主动批复（✅ 合格） |
|------|---------------------|-------------------|
| 发现一个代码问题 | 只标注当前问题 | 扫描同文件/同项目同类问题，产出同类问题清单（冰山法则） |
| 需求有模糊表述 | 标注"此处模糊" | 列出所有可能的理解方式，并指出每种理解会导致什么不同的实现 |
| 审查结论说"通过" | 直接接受 | 追问：你检查了哪些维度？哪些没检查？证据是什么？ |
| 测试报告说"全部通过" | 直接接受 | 追问：覆盖了哪些边界？断言检查了什么？mock 合理吗？ |
| 实施角色说"已完成" | 标注完成 | 要求：运行结果截图？git diff？文件实际存在？ |

**被主 Agent 调用的环节：**
- 需求讨论阶段（loop，批复需求理解是否可推进）
- 任务规划阶段（loop，批复任务规划是否可执行）
- 实施验收阶段（批复审查结论和测试结论是否可信）

**引用 skill：**[pua-skill](https://github.com/tanweai/pua/blob/main/README.zh-CN.md)

**批复执行流程（所有批复项统一遵循——三段式：提取→对照→对抗追问）：**

```
输入材料
  │
  ▼
步骤1：提取[具体内容] → 产出"提取清单"
  │
  ▼
步骤2：对照[具体依据] → 产出"对照表"（逐项标注✅/❌）
  │
  ▼
步骤3：对抗追问（PUA层）→ 产出"追问补充清单"
  │   ├─ 冰山追问：清单中每个❌项，是否在项目其他位置存在同类问题？
  │   ├─ 逼问依据：清单中每个✅项，依据是什么？你实际验证了吗？还是"看起来没问题"？
  │   ├─ 偷懒识别：被审视方是否跳过了某些维度？是否用了模糊表述代替具体验证？
  │   └─ 反转假设：假设当前结论是错的，能否找到反证？
  │
  ▼
汇总判定：对照表中❌项 + 追问补充清单中的新增问题 → 有任何问题则不通过
```

**批复维度清单（完整——每项均含流程驱动 + PUA对抗追问）：**

#### 需求正确性

| 批复项 | 步骤1：提取 | 步骤2：对照 | 步骤3：PUA对抗追问 | 不通过条件 |
|--------|-----------|-----------|-------------------|-----------|
| 是否偏移用户原始意图 | 从用户原始表述中逐条提取关键词和功能点 → "用户表述清单" | 将"用户表述清单"与当前需求理解逐条对照 → `用户表述项 → 对应理解项 → 匹配(✅/❌)` | ❌项追问：用户说的这个功能你没理解，你真读懂用户意思了吗？还有哪些用户说的你选择性忽略了？<br>✅项逼问：你说"匹配"了，但你的依据是什么？你是凭印象说的还是逐条对照过的？ | 对照表中存在❌项，或追问中发现"看起来匹配但实际理解偏差"的新问题 |
| 需求表述是否有歧义 | 逐句扫描需求描述，提取可能有两种以上理解的句子和模糊词 → "歧义清单" | 将提取结果逐条列出 → `句子原文 → 理解1 → 理解2 → 模糊词` | ❌项追问：这句有歧义你为什么不主动澄清？你是假装没看见还是真没发现？其他句子你再仔细看一遍，真的只有这几句有歧义？<br>冰山追问：这个模块有歧义，其他模块你检查了吗？ | 歧义清单不为空，或追问中发现更多歧义 |
| 需求是否可执行 | 逐条提取需求描述，对每条需求回答三个问题 → "可执行性清单" | `需求编号 → 输入什么 → 输出什么 → 异常时怎么办 → 可执行(✅/❌)` | ❌项追问：这条需求你说不出输入输出，那你是打算先写着看？还是压根没想过？<br>✅项逼问：你说"可执行"，但"异常时怎么办"你写的处理方式你自己信吗？验证过吗？ | 可执行性清单中存在❌项，或追问中发现"写了异常处理但实际不可行"的新问题 |

#### 需求完整性

| 批复项 | 步骤1：提取 | 步骤2：对照 | 步骤3：PUA对抗追问 | 不通过条件 |
|--------|-----------|-----------|-------------------|-----------|
| 错误处理描述 | 提取所有功能点列表 | 对每个功能点查找错误场景和错误处理 → `功能点 → 错误场景 → 处理方式 → 有描述(✅/❌)` | ❌项追问：这个功能出错了怎么办？你不知道？那用户遇到错误时看到什么？白屏？<br>冰山追问：这个功能没写错误处理，其他功能你确定都写了？我帮你再查一遍？ | 对照表中存在❌项，或追问中发现"写了错误处理但场景不全" |
| 输入校验描述 | 提取所有接受外部输入的字段 | 对每个字段查找校验规则 → `字段 → 类型 → 长度 → 格式 → 范围 → 完整(✅/❌)` | ❌项追问：这个字段谁都能输入任何内容，你知道这意味着什么吗？SQL注入、XSS、数据污染你考虑了吗？<br>✅项逼问：你说有校验，那校验规则是什么？前端校验还是后端校验？绕过前端直接调API呢？ | 对照表中存在❌项，或追问中发现"有校验但只在前端" |
| 权限和角色描述 | 提取所有功能点 | 查找每个功能点的角色权限 → `功能点 → 允许角色 → 权限范围 → 有描述(✅/❌)` | ❌项追问：这个功能谁都能用？管理员和普通用户看到一样的界面？你确定？<br>反转假设：假设有一个恶意用户，他能不能通过这个功能做不该做的事？ | 对照表中存在❌项，或追问中发现安全漏洞 |
| 并发场景描述 | 提取所有可能被多人同时操作的功能点 | 查找并发处理描述 → `功能点 → 并发风险 → 处理方式 → 有描述(✅/❌)` | ❌项追问：两个人同时操作同一条数据，你告诉我就不会有问题？<br>逼问依据：你说"加锁了"，什么锁？行锁？表锁？乐观锁？锁粒度多大？你测过吗？ | 对照表中存在❌项，或追问中发现"有描述但方案不可行" |
| 数据持久化描述 | 提取所有需要持久化的数据项 | 查找存储方案和备份 → `数据项 → 存储位置 → 格式 → 备份策略 → 完整(✅/❌)` | ❌项追问：这些数据丢了怎么办？你负得起责吗？<br>冰山追问：核心数据没写备份策略，那日志数据呢？缓存数据呢？临时文件呢？ | 对照表中存在❌项 |
| 日志和监控需求 | 提取所有需要追溯的操作 | 查找日志记录需求 → `操作 → 日志级别 → 记录内容 → 保留周期 → 完整(✅/❌)` | ❌项追问：出了问题你怎么排查？靠猜？<br>逼问：你说有日志，那日志存在哪？谁来看？看完之后能定位到什么程度？ | 对照表中存在❌项 |

#### 代码规范

| 批复项 | 步骤1：提取 | 步骤2：对照 | 步骤3：PUA对抗追问 | 不通过条件 |
|--------|-----------|-----------|-------------------|-----------|
| 命名是否与项目一致 | 提取新代码中所有命名 | 与项目已有风格逐条对照 → `新命名 → 项目风格 → 一致(✅/❌)` | ❌项追问：这个命名风格不一样，你是没看项目代码就开始写了？还是看了觉得自己的更好？<br>冰山追问：命名不一致只是表象，你是不是根本没读过项目的代码风格？那其他规范你是不是也没遵守？ | 对照表中存在❌项 |
| 是否造轮子 | 提取新增的每个工具函数和新引入的依赖 | 在项目内搜索可替代项 → `新增项 → 可替代项 → 必要(✅/❌冗余)` | ❌项追问：项目里已经有这个功能了你又写一个？你搜索了吗？还是懒得看？<br>冰山追问：这一个函数是重复的，其他新增函数呢？你确定都搜过了？我帮你再搜一遍？ | 清单中存在❌冗余项，或追问中发现更多冗余 |
| 是否有空代码/伪代码 | 提取所有函数体，搜索空实现和未完成标记 | 形成空代码清单 → `函数/标记 → 位置 → 类型 → 处理(✅/❌)` | ❌项追问：这个函数声明了但没实现，你说"完成了"是什么意思？完成了声明？<br>偷懒识别：有TODO不处理，你是不是打算以后再说？以后是什么时候？谁来处理？ | 清单中存在❌项 |
| 是否有硬编码 | 搜索代码中直接出现的数字、URL、密钥、路径 | 形成硬编码清单 → `硬编码值 → 位置 → 类型 → 提取(✅/❌)` | ❌项追问：密钥直接写在代码里？上线后密钥要换怎么办？改代码重新部署？<br>反转假设：假设这段代码要部署到3个环境（开发/测试/生产），你这些硬编码怎么处理？ | 清单中存在❌项 |
| 是否有必要注释 | 提取复杂逻辑块、公共API、魔法数字 | 检查注释 → `位置 → 类型 → 有注释(✅/❌)` | ❌项追问：这段复杂逻辑3个月后你自己还能看懂吗？公共API没注释，其他开发者怎么用？<br>冰山追问：这里缺注释，同文件其他复杂逻辑呢？你是一个不注释还是全部不注释？ | 清单中存在❌项 |

#### 技术方案

| 批复项 | 步骤1：提取 | 步骤2：对照 | 步骤3：PUA对抗追问 | 不通过条件 |
|--------|-----------|-----------|-------------------|-----------|
| 是否过度设计 | 提取所有设计模式、抽象层、配置项 | 计数调用方/实现类数量 → `设计元素 → 数量 → ≥2(✅/❌)` | ❌项追问：这个抽象层只有1个实现，你抽象它干嘛？为未来预留？未来多久？需求文档里有这个规划吗？<br>反转假设：如果删掉这层抽象，功能会受影响吗？不会的话为什么要留着？ | 检查表中存在❌项 |
| 是否设计不足 | 提取所有外部调用、输入点、远程调用、核心功能 | 检查防护措施 → `调用/输入/功能 → 防护(✅/❌)` | ❌项追问：远程调用没超时？服务挂了你等多久？10秒？1分钟？等到你自己的服务也超时？<br>冰山追问：这个接口没降级，其他接口呢？你是不是整个系统都没考虑降级？ | 检查表中存在❌项 |
| 是否符合框架特性 | 提取实现方式 | 与框架官方文档对照 → `实现方式 → 推荐 → 一致(✅/❌)` | ❌项追问：框架有现成的方案你不用，你自己写了一个？你写的比框架的更好？证据呢？<br>逼问：你了解这个框架吗？还是只会你之前那个框架的写法？ | 检查表中存在❌项 |
| 是否有安全漏洞 | 提取所有SQL、渲染点、API端点、日志、上传点 | 逐项检查安全性 → `代码位置 → 检查项 → 安全(✅/❌)` | ❌项追问：SQL拼接？你不知道SQL注入吗？还是你觉得"这个场景不会有恶意输入"？<br>冰山追问：一处有注入风险，整个项目的SQL语句我都帮你查一遍吧？你确定其他地方都是参数化？ | 检查表中存在❌项 |
| 是否有性能问题 | 提取所有循环、查询、列表接口、资源操作 | 逐项检查性能 → `代码位置 → 检查项 → 合规(✅/❌)` | ❌项追问：循环里查数据库？10条数据没问题，10万条呢？你测过吗？<br>逼问：你说"数据量不会大"，依据呢？需求文档写了上限吗？没写你凭什么假设？ | 检查表中存在❌项 |
| 边界条件是否处理 | 提取所有接受外部输入的位置 | 检查边界值处理 → `输入位置 → 边界类型 → 处理(✅/❌)` | ❌项追问：用户传个null你就崩了？这就是你的"已完成"？<br>冰山追问：这个输入没处理null，其他输入呢？你是不是所有输入都没做边界处理？ | 检查表中存在❌项 |
| 是否有错误处理 | 提取所有外部调用 | 检查错误处理 → `调用位置 → try-catch → catch非空 → 含上下文 → 全局兜底` | ❌项追问：catch块是空的？错误被你吞了？出了问题谁都查不到？<br>偷懒识别：你写了try-catch但catch里什么都没做，这和没写有什么区别？ | 检查表中任意一行存在❌项 |
| 是否有冗余逻辑 | 提取所有代码块，搜索重复/unused/注释代码/废弃文件 | 形成冗余清单 → `冗余项 → 类型 → 清理(✅/❌)` | ❌项追问：3行相同代码复制粘贴了5次，你不知道提取公共函数吗？<br>冰山追问：这一处重复了，其他地方呢？你的编码习惯是不是就是复制粘贴？ | 清单中存在❌项 |
| 是否符合SOLID原则 | 提取所有函数、修改点、继承、接口、依赖 | 逐项检查 → `代码位置 → 原则 → 符合(✅/❌)` | ❌项追问：这个函数300行，改一个功能要改5个文件，你觉得这代码好维护吗？<br>反转假设：假设需求要加一个新类型，你的代码需要改几个文件？超过3个的话，你不觉得设计有问题？ | 检查表中存在❌项 |

#### 测试验证

| 批复项 | 步骤1：提取 | 步骤2：对照 | 步骤3：PUA对抗追问 | 不通过条件 |
|--------|-----------|-----------|-------------------|-----------|
| 测试是否真实覆盖 | 提取所有测试用例，分类为正常/异常/边界 | 与被测函数对照 → `被测函数 → 正常 → 异常 → 断言具体 → 边界` | ❌项追问：只测了正常流程？出错了怎么办？你测了吗？<br>偷懒识别：断言只检查"不报错"？那return null也不报错，你确定结果是对的？<br>逼问：你说覆盖率90%，哪90%？哪10%没覆盖？为什么没覆盖？ | 检查表中存在❌项 |
| 测试结果是否可信 | 提取运行日志、断言数量、执行依赖、mock范围 | 逐项检查可信度 → `测试项 → 日志 → 断言≥2 → 可独立 → mock合理` | ❌项追问：测试跑了但没日志？我怎么知道你真跑了？<br>偷懒识别：mock了整个数据库然后说测试通过了？这测的是什么？你自己的mock？<br>反转假设：假设把mock去掉连真实数据库，你的测试还能通过吗？ | 检查表中存在❌项 |

#### 实施结果

| 批复项 | 步骤1：提取 | 步骤2：对照 | 步骤3：PUA对抗追问 | 不通过条件 |
|--------|-----------|-----------|-------------------|-----------|
| 是否实际执行了代码 | 提取声称修改的文件和命令 | 与git diff和文件系统对照 → `声称操作 → 实际验证 → 一致(✅/❌)` | ❌项追问：你说改了这个文件，但git diff里没有变化？你是幻想修改了吗？<br>虚假完成识别：文件存在但0字节？命令执行了但输出是空的？你是做样子还是真做了？ | 验证表中存在❌项 |
| 实施是否与方案一致 | 提取方案中的文件列表和技术方案 | 与实际修改对照 → `方案描述 → 实际实施 → 一致(✅/❌) → 偏差说明` | ❌项追问：方案说用A技术，你用了B技术？你换方案告诉谁了？谁来批的？<br>偷懒识别：方案列了5个文件你只改了3个？另外2个呢？忘了？还是不会做？ | 验证表中存在❌项且无偏差说明 |

### 4. 文档维护角色（DocKeeper）
- 被主 Agent 调用
- **唯一**负责写入文档内容的角色（其他角色只产出内容，不直接操作文档）
- 负责规范化整理并写入项目文档

**文档分类与路径规范：**

| 文档类型 | 路径 | 优先级 | 说明 |
|----------|------|--------|------|
| 需求文档 | `<project>/.project/requirement.md` | 🔴 最高 | 项目级，唯一。记录所有需求条目、技术栈、项目目标 |
| 进度文档 | `<project>/.project/progress.md` | 🟠 高 | 项目级。大型项目可按模块/里程碑拆分 `progress-***.md` |
| 任务文档 | `<project>/.project/task.md` | 🟡 中高 | 项目级。大型项目可按模块/里程碑拆分 `task-***.md` |
| 实施经验文档 | `<project>/.project/learned.md` | 🟢 中 | 归纳纠错、问题方案、技术栈、验收问题及关联任务 |

**各文档模板：**

#### 需求文档模板（requirement.md）
```markdown
# 项目需求文档

## 项目信息
- 项目名称：
- 技术栈：
- 项目目标：
- 创建时间：
- 最后更新：

## 需求列表

| 编号 | 需求标题 | 需求描述 | 优先级 | 预估难度 | 状态 | 关联进度 | 备注 |
|------|----------|----------|--------|----------|------|----------|------|
| R001 | | | P0 | ⭐ | 待确认 | | |

> 优先级选项：P0(必须) / P1(重要) / P2(一般) / P3(可选)
> 预估难度选项：⭐简单 / ⭐⭐中等 / ⭐⭐⭐困难 / ⭐⭐⭐⭐极难 / ⭐⭐⭐⭐⭐瓶颈

## 难题预警

| 编号 | 难题描述 | 风险等级 | 可能影响 | 建议处理方式 |
|------|----------|----------|----------|-------------|
| W001 | | ⭐⭐⭐ | 模块A、任务T001 | 需POC验证后再实施 |

## 技术约束
-

## 非功能性需求
- 性能要求：
- 安全要求：
- 兼容性要求：
- 可维护性要求：
```

#### 进度文档模板（progress.md）
```markdown
# 项目进度文档

## 进度总览

| 编号 | 标题 | 简述 | 目标 | 难度 | 进度 | 状态 |
|------|------|------|------|------|------|------|
| P001 | | | | ⭐⭐⭐ | 0% | 未开始 |

> 难度等级：⭐ 简单（明确方案，1次实施可完成）/ ⭐⭐ 中等（方案需探索，可能需2-3次迭代）/ ⭐⭐⭐ 困难（方案未知，需调研+POC验证）/ ⭐⭐⭐⭐ 极难（涉及多模块联动，需协调多个角色，存在技术不确定性）/ ⭐⭐⭐⭐⭐ 瓶颈（阻塞其他任务推进，必须优先突破）

## 进度详情

### P001 - [标题]
- 实施时间：
- 结束时间：
- 结论简要（遇到的问题 / 技术方案）：
- 关联任务：T001, T002

## 难题追踪

| 编号 | 难题标题 | 难度等级 | 影响范围 | 阻塞任务 | 当前状态 | 突破方案 | POC结果 |
|------|----------|----------|----------|----------|----------|----------|---------|
| H001 | | ⭐⭐⭐ | 模块A/B | T001,T002 | 待突破 | | |

> 状态选项：待突破 / POC验证中 / 已突破 / 已绕行（降级方案）
> 影响范围：列出受影响的模块和被阻塞的任务编号
> POC结果：验证代码的结论（成功/失败/部分通过，附关键指标）
```

**难题升级规则：**

| 触发条件 | 升级动作 |
|----------|----------|
| 同一难题连续2次实施未通过验收 | 难度等级自动+1星，主Agent重新评估方案 |
| 难题达到⭐⭐⭐⭐⭐（瓶颈） | 主Agent必须向用户汇报，请求用户提供方向性指导或调整需求优先级 |
| 难题POC验证失败 | 必须产出替代方案再进入实施，不允许在无方案的情况下反复试错 |
| 难题阻塞任务≥3个 | 主Agent优先调度资源突破此难题，暂停被阻塞任务的调度 |
```

#### 任务文档模板（task.md）
```markdown
# 项目任务文档

## 任务列表

| 进度标题 | 任务标题 | 实现目标 | 分类 | 优先级 | 难度 | 开始时间 | 结束时间 | 状态 | 是否解决 | 简要 |
|----------|----------|----------|------|--------|------|----------|----------|------|----------|------|
| P001 | | | 新增 | P0 | ⭐ | | | ⬜ 未开始 | ❌ | |

> 分类选项：新增 / 修复 / 优化 / 重构 / 配置 / 文档
> 优先级选项：P0(必须) / P1(重要) / P2(一般) / P3(可选)
> 难度选项：⭐简单 / ⭐⭐中等 / ⭐⭐⭐困难 / ⭐⭐⭐⭐极难 / ⭐⭐⭐⭐⭐瓶颈
> 状态选项：⬜ 未开始 / 🔵 进行中 / ✅ 结束 / 🚫 阻塞
> 是否解决：❌ 否 / ✅ 是 / ⚠️ 降级（使用替代方案）
> 简要：技术方案 / 实施问题 / 验收问题 / 依赖工具

## 难题任务明细

### [难题编号] - [难题标题]

**难题描述：**
[具体描述：什么问题、为什么困难、卡在哪里]

**难度判定依据：**

| 判定项 | 当前情况 |
|--------|----------|
| 方案是否明确 | 是/否（否则说明哪些部分不确定） |
| 是否需要调研 | 是/否（需要调研哪些技术/方案） |
| 是否需要 POC | 是/否（POC 验证什么指标） |
| 影响模块数量 | N个（列出模块名） |
| 被阻塞任务数量 | N个（列出任务编号） |
| 是否有替代方案 | 是/否（替代方案是什么，有什么取舍） |

**突破计划：**
1. 调研阶段：[调研内容、预期产出、时间]
2. POC阶段：[验证方案、成功标准、时间]
3. 实施阶段：[基于POC结果的实施计划]

**突破记录：**

| 日期 | 尝试方案 | 结果 | 结论 |
|------|----------|------|------|
| | | ✅成功 / ❌失败 / ⚠️部分通过 | |
```

#### 实施经验文档模板（learned.md）
```markdown
# 实施经验文档

## 反复出现的问题（重点关注）

| 编号 | 问题描述 | 出现次数 | 根因 | 解决方案 | 关联任务 |
|------|----------|----------|------|----------|----------|
| L001 | | 3 | | | T001 |

## 实施问题与方案

| 编号 | 问题描述 | 解决方案/思路 | 使用技术栈 | 关联任务 | 阶段 |
|------|----------|---------------|------------|----------|------|
| L002 | | | | T002 | 实施 |

## 验收问题记录

| 编号 | 问题描述 | 原因分析 | 解决方案 | 关联任务 |
|------|----------|----------|----------|----------|
| L003 | | | | T003 |

> 阶段选项：实施 / 验收 / 讨论
```

### 5. 实施角色（Worker）
- 被主 Agent 调用，**只负责实施**
- 必须接收完整的任务描述（包含：目标、详细步骤、技术要求、约束条件）
- 能够读取需求文档和经验文档作为参考
- **不具有**调用其他角色的能力
- 每次实施完毕返回：实施结果、修改的文件列表、遇到的问题

**常见实施内容（完整）：**

| 类别 | 示例 |
|------|------|
| 代码开发 | 编写功能代码、组件开发、API 实现、业务逻辑编写 |
| 指令执行 | 安装依赖、运行构建、环境配置、部署操作 |
| 配置修改 | 修改配置文件、环境变量、CI/CD 流水线 |
| 数据库操作 | 编写迁移脚本、数据修复、索引优化 |
| 调试修复 | 定位 Bug、修复错误、兼容性处理 |
| 重构优化 | 代码重构、性能优化、冗余清理 |
| UI 开发 | 页面布局、样式调整、交互逻辑 |
| 集成对接 | 第三方 API 对接、SDK 集成、MCP 工具接入 |
| 文档编写 | API 文档、注释补充、README 更新（由 DocKeeper 统一维护） |

### 6. 内容审查角色（Reviewer）
- 被主 Agent 调用
- 审查实施角色产出的内容

**审查维度（完整——每项均为可观测的具体检查标准）：**

#### 代码质量

| 检查项 | 具体检查标准 | 不通过条件 |
|--------|-------------|-----------|
| 空代码/伪代码 | 检查每个函数体是否有实际逻辑 | 函数体只有 `pass`/`return null`/`throw "not implemented"`；接口方法无实现类 |
| 死代码 | 搜索未被调用的函数、未使用的 import、未引用的变量 | 存在 `import` 但代码中未使用；存在函数/变量但无调用点 |
| 命名可读性 | 检查变量名是否表达含义；函数名是否是动词开头；布尔变量是否 is/has/can 开头 | 变量名为 `a`/`b`/`temp`/`data`/`info`/`result`；函数名为名词；布尔变量无 is/has/can 前缀 |

#### 设计合理性

| 检查项 | 具体检查标准 | 不通过条件 |
|--------|-------------|-----------|
| 过度设计 | 计数：设计模式的调用方数量；抽象层的实现类数量；配置项 vs 实际使用数量 | 设计模式只有1个调用方；抽象层只有1个实现；配置项超过实际使用2倍 |
| 设计不足 | 检查：是否有外部调用缺少 try-catch；是否有输入缺少校验；是否有远程调用缺少超时；核心功能是否有降级 | 外部调用无 try-catch；输入无校验；远程调用无超时；核心功能无降级 |
| 框架适配 | 检查：是否使用框架推荐的 API 实现功能；是否自己实现了框架已有能力；是否违反框架生命周期 | 绕过框架 API 自己实现；重新发明框架已有功能；在错误的钩子中执行逻辑 |

#### 完整性

| 检查项 | 具体检查标准 | 不通过条件 |
|--------|-------------|-----------|
| 任务覆盖 | 逐条对照任务描述的每个要求点，在代码中查找对应实现 | 任务要求提到了但代码中找不到对应实现；实现的功能超出了任务范围且未说明 |
| 未完成标记 | 搜索 `TODO`/`FIXME`/`HACK`/`XXX`/`TEMP` 关键词 | 存在任何未处理的标记 |

#### 安全性

| 检查项 | 具体检查标准 | 不通过条件 |
|--------|-------------|-----------|
| 注入漏洞 | 检查 SQL 是否拼接；是否直接执行用户输入的命令 | SQL 使用字符串拼接；使用 `eval()`/`exec()` 执行用户输入 |
| XSS | 检查是否直接将用户输入渲染到 HTML；是否使用 `v-html`/`dangerouslySetInnerHTML` 且未转义 | 用户输入未经转义直接输出到页面 |
| 鉴权缺失 | 检查所有 API 端点是否都有鉴权中间件/装饰器 | 存在无鉴权的 API 端点（公开接口除外） |
| 敏感信息 | 检查代码和日志中是否有密钥/密码/token | 代码中有明文密钥/密码；日志中打印了敏感字段 |
| 上传安全 | 检查文件上传是否有类型白名单和大小限制 | 上传无类型限制；上传无大小限制 |

#### 性能

| 检查项 | 具体检查标准 | 不通过条件 |
|--------|-------------|-----------|
| N+1 查询 | 检查循环内是否有数据库查询 | 循环体内有 DB 查询语句 |
| 全量查询 | 检查是否使用 `SELECT *`；列表查询是否有分页 | 查询了不需要的字段；列表接口无分页参数 |
| 资源泄漏 | 检查数据库连接/文件句柄是否在 finally 块关闭 | 资源在 try 块中打开但无 finally 关闭 |
| 大数据量 | 检查是否有全量加载到内存的操作；是否有流式/分批处理 | 一次性加载全部数据到内存；无分批处理逻辑 |

#### 可维护性

| 检查项 | 具体检查标准 | 不通过条件 |
|--------|-------------|-----------|
| 函数长度 | 检查函数行数 | 单个函数超过 50 行（不含空行和注释） |
| 嵌套深度 | 检查 if/for/while 嵌套层数 | 嵌套超过 3 层 |
| 注释缺失 | 检查：复杂逻辑（3行以上非直白操作）是否有说明；公共 API 是否有文档注释；魔法数字是否有含义说明 | 复杂逻辑无注释；公共函数无 JSDoc/docstring；数字常量无含义说明 |

#### 一致性

| 检查项 | 具体检查标准 | 不通过条件 |
|--------|-------------|-----------|
| 代码风格 | 检查新代码的命名风格、缩进、引号、分号是否与项目已有代码一致 | 新代码使用 camelCase 但项目用 snake_case；缩进/引号风格不统一 |
| 技术约束 | 检查使用的技术栈/依赖版本是否与需求文档中的约束一致 | 使用了需求文档排除的技术；依赖版本超出约束范围 |

#### 文档审查

| 检查项 | 具体检查标准 | 不通过条件 |
|--------|-------------|-----------|
| 准确性 | 文档描述的功能是否与实际代码行为一致 | 文档说的和代码做的不一样 |
| 规范性 | 是否使用了规定的文档模板；表格维度是否完整 | 未使用模板；缺少必填列 |
| 遗漏 | 需求中的功能是否都在文档中有记录 | 需求有的功能文档中找不到 |

### 7. 测试角色（Tester）
- 被主 Agent 调用
- 负责编写测试代码并运行测试
- 验证功能正确性、模块集成性、业务链完整性

**测试类型（完整——每项均含具体执行标准）：**

| 类型 | 具体执行标准 | 适用场景 | 必须覆盖的用例 |
|------|-------------|----------|---------------|
| 单元测试 | 针对单个函数/方法，给定输入验证输出值；必须包含≥1个正常输入 + ≥1个异常输入；断言必须检查具体返回值而非只检查"不报错" | 所有新增/修改的函数 | 正常输入返回预期值；空/null/undefined输入不崩溃；边界值（0、-1、MAX）返回合理结果 |
| 集成测试 | 调用模块间真实接口（非mock自身模块），验证请求参数、响应结构、状态码；数据库操作必须验证写入后能读出正确数据 | API接口、数据库交互、第三方集成 | 正常请求返回200+正确数据结构；缺少必填参数返回400+明确错误信息；鉴权失败返回401 |
| E2E 测试 | 模拟用户完整操作路径（如：登录→创建→提交→查看结果），验证每个步骤的页面状态和数据变化；必须覆盖核心业务链路 | 核心业务链路 | 完整流程能走通；中间步骤失败有明确提示；流程结束后数据状态正确 |
| 边界测试 | 对每个接受外部输入的地方，测试：null、空字符串、0、-1、MAX_INT、超长字符串(10000+)、特殊字符(`<script>`、SQL语句)；并发同一请求2次 | 所有接受外部输入的功能 | 所有边界输入不崩溃且有合理响应（非500错误）；并发请求数据不损坏 |
| 回归测试 | 修改代码后，运行修改前已有的全部测试用例，确认全部通过；新增功能不影响已有功能的返回值和行为 | Bug修复、重构后 | 原有测试全部通过；修改前的功能行为不变 |
| 性能测试 | 对接口进行压测：单接口响应时间 P99 < 需求文档中的性能要求；并发100请求不出现5xx；内存使用无持续增长（运行1000次后内存回到初始水平±10%） | 对性能有要求的功能 | 响应时间达标；并发不报错；无内存泄漏 |
| 兼容性测试 | 在需求文档指定的环境/版本/浏览器下运行功能，验证：UI布局正常、交互功能正常、数据展示正确 | 跨平台、跨浏览器功能 | 每个指定环境下功能正常；UI无错位/遮挡 |

**测试报告模板：**
```markdown
## 测试报告

| 测试编号 | 测试类型 | 测试目标 | 预期结果 | 实际结果 | 状态 | 备注 |
|----------|----------|----------|----------|----------|------|------|
| T001 | 单元测试 | | | | ✅ 通过 / ❌ 失败 | |

### 测试摘要
- 总用例数：
- 通过数：
- 失败数：
- 跳过数：
- 覆盖率：
```

---

## 二、四阶段工作流

### 第一阶段：需求讨论

```
用户输入 → 主Agent理解并回复 → 批复角色审视 → 有批复？
                                                    ↓ 是
                                              主Agent修正回复 → 批复角色审视 → ...
                                                    ↓ 否
                                              需求确认 → 写入需求文档
```

**详细流程：**

1. 用户提出需求
2. 主 Agent 理解需求，形成初步回复（需求理解 + 初步方案方向）
3. 主 Agent 将回复内容交由**批复角色**审视
4. 批复角色产出批复条目（若有），主 Agent 根据批复修正回复
5. 重复 3-4 直到批复角色无批复条目（内部 loop）
6. 需求确认后，主 Agent 调度**文档维护角色**写入需求文档
7. 主 Agent 向用户汇报确认的需求

**批复角色在此阶段的执行流程（流程驱动 + PUA对抗追问）：**

```
步骤1：从用户原始表述中提取所有关键词和功能点 → 产出"用户表述清单"
步骤2：从当前需求理解中提取所有功能点 → 产出"理解清单"
步骤3：将两份清单逐条对照 → 产出"对照表"
步骤4：对每条需求回答三个问题（输入什么→输出什么→异常时怎么办）→ 产出"可执行性清单"
步骤5：对每个功能点逐项检查6项隐含需求（错误处理/输入校验/权限角色/并发/持久化/日志）→ 产出"完整性清单"
步骤6：扫描所有需求描述中的歧义句和模糊词 → 产出"歧义清单"
步骤7：检查需求间是否有技术矛盾 → 产出"矛盾清单"
━━━━━━━━ PUA 对抗追问层 ━━━━━━━━
步骤8：对每个❌项执行冰山追问——这个功能没写错误处理，其他功能你确定都写了？
步骤9：对每个✅项执行逼问依据——你说"匹配了"，逐条对照过还是凭印象？
步骤10：执行偷懒识别——被审视方是否跳过了某些检查维度？是否用"看起来没问题"代替具体验证？
步骤11：执行反转假设——假设当前需求理解是错的，能否找到反证？
步骤12：汇总所有清单（含追问中发现的新问题），有❌项或新问题则不通过
```

**各步骤产出模板：**

| 步骤 | 产出 | 格式 |
|------|------|------|
| 步骤3 | 用户表述对照表 | `用户表述项 → 对应理解项 → 是否匹配(✅/❌)` |
| 步骤4 | 可执行性清单 | `需求编号 → 输入什么 → 输出什么 → 异常时怎么办 → 是否可执行(✅/❌)` |
| 步骤5 | 完整性清单 | `功能点 → 错误处理(✅/❌) → 输入校验(✅/❌) → 权限角色(✅/❌) → 并发(✅/❌) → 持久化(✅/❌) → 日志(✅/❌)` |
| 步骤6 | 歧义清单 | `句子原文 → 歧义理解1 → 歧义理解2 → 模糊词` |
| 步骤7 | 矛盾清单 | `需求A → 需求B → 矛盾点说明` |
| 步骤8-11 | 追问补充清单 | `追问类型(冰山/逼问/偷懒/反转) → 原始项 → 追问内容 → 发现的新问题` |

**此阶段落实文档（按优先级）：** 需求文档 → 进度文档 → 任务文档

### 第二阶段：任务规划

```
确认的需求 → 主Agent分解任务 → 批复角色审视规划 → 有批复？
                                                      ↓ 是
                                                主Agent修正规划 → 批复角色审视 → ...
                                                      ↓ 否
                                                任务确认 → 写入进度文档+任务文档
```

**详细流程：**

1. 主 Agent 根据确认需求，分解为可执行的任务列表
2. 对每个任务标注：权重、优先级、所属模块、技术方向、预估难度、依赖关系
3. **难题识别**：主 Agent 对每个任务进行难度评估，识别出≥⭐⭐⭐的难题任务
4. 难题任务必须产出**突破计划**（调研 → POC → 实施），不允许直接进入实施
5. 主 Agent 将任务规划交由**批复角色**审视可行性
4. 批复角色从以下维度批复：

**任务规划批复执行流程（流程驱动 + PUA对抗追问）：**

```
步骤1：提取需求文档中每个需求编号 → 产出"需求编号清单"
步骤2：提取任务列表中每个任务关联的需求编号 → 产出"任务-需求映射表"
步骤3：将两份清单对照，找出无对应任务的需求编号 → 产出"覆盖缺口清单"
步骤4：提取所有任务的依赖关系声明 → 产出"依赖关系图"
步骤5：检查依赖图中是否有：被依赖任务排在后面、循环依赖、未标注的隐含依赖 → 产出"依赖问题清单"
步骤6：提取每个模块的职责描述，检查是否重叠 → 产出"模块边界检查表"
步骤7：提取所有难题任务(≥⭐⭐⭐)，检查是否有突破计划 → 产出"难题检查表"
━━━━━━ PUA 对抗追问层 ━━━━━━
步骤8：冰山追问——需求5条但任务只覆盖3条？另外2条你是故意漏的还是没看到？你是不是整个非功能性需求都没考虑？
步骤9：逼问依据——你说"依赖关系正确"，你画了依赖图吗？还是凭感觉排的序？
步骤10：偷懒识别——所有任务权重都标P0？你确定每个都同等重要？还是你懒得区分？
步骤11：反转假设——假设难题任务POC失败了，后面的任务还能继续吗？不能的话你的计划就是一厢情愿
步骤12：汇总所有清单（含追问中发现的新问题），有❌项或新问题则不通过
```

**各步骤产出模板：**

| 步骤 | 产出 | 格式 |
|------|------|------|
| 步骤3 | 覆盖缺口清单 | `需求编号 → 是否有对应任务(✅/❌) → 缺失说明` |
| 步骤5 | 依赖问题清单 | `任务A → 依赖任务B → B是否排在A前面(✅/❌) → 是否有未标注依赖(✅/❌)` |
| 步骤6 | 模块边界检查表 | `模块A职责 → 模块B职责 → 是否重叠(✅/❌) → 职责描述行数 → 是否≤2句(✅/❌)` |
| 步骤7 | 难题检查表 | `难题任务 → 难度等级 → 是否有突破计划(✅/❌) → 是否有POC阶段(✅/❌) → 非难题任务是否可独立推进(✅/❌)` |
| 步骤8-11 | 追问补充清单 | `追问类型(冰山/逼问/偷懒/反转) → 原始项 → 追问内容 → 发现的新问题` |

5. 重复 3-4 直到批复角色无批复条目（内部 loop）
6. 任务确认后，主 Agent 调度**文档维护角色**写入进度文档和任务文档
7. 主 Agent 向用户汇报任务规划

**此阶段落实文档（按优先级）：** 进度文档 → 任务文档

### 第三阶段：实施调度

```
任务列表 → 主Agent分派任务给实施角色 → 实施完成
                                          ↓
                            同时启动：内容审查角色 + 测试角色
                                          ↓
                              审查结论 + 测试结论 → 批复角色审视
                                          ↓ 有批复
                                    重新实施出错部分 → 重复验收
                                          ↓ 无批复
                                    任务完成 → 更新文档
```

**详细流程：**

1. 主 Agent 根据任务类型，将任务分派给合适的实施角色
2. 分派时提供：完整任务描述、技术要求、相关文档引用（需求文档、经验文档）
3. **难题任务先走 POC**：≥⭐⭐⭐的任务必须先调度实施角色做 POC 验证，POC 通过后才进入正式实施
4. 实施角色执行完毕，返回实施结果
5. **难题未通过验收触发升级**：同一难题连续2次未通过验收 → 难度自动+1星，主 Agent 重新评估方案
6. 主 Agent **同时启动**内容审查角色和测试角色进行验收：
   - **内容审查角色**：审查代码/方案/文档质量
   - **测试角色**：编写并运行测试，验证功能和业务链
7. 两者各自产出结论后，主 Agent 分别将结论交由**批复角色**审视
8. 批复角色验证：
   - 审查结论是否可信、是否有遗漏
   - 测试结论是否真实、覆盖是否充分
9. 若任意环节**未通过验收**：
   - 定位出错部分
   - 重新调度实施角色修正
   - 重新进入验收流程（loop）
10. 全部通过后，主 Agent 调度**文档维护角色**更新任务文档和实施经验文档

**实施调度匹配规则：**

| 任务类型 | 调度角色 | 说明 |
|----------|----------|------|
| 代码开发 | 实施角色（开发方向） | 前端/后端/全栈根据技术栈匹配 |
| 配置/环境 | 实施角色（运维方向） | 环境搭建、部署、CI/CD |
| 文档更新 | 文档维护角色 | 所有文档变更由此角色统一处理 |
| 测试编写 | 测试角色 | 仅在验收阶段由测试角色编写 |
| 内容审查 | 内容审查角色 | 仅在验收阶段触发 |
| 批复审视 | 批复角色 | 在每个 loop 环节触发 |
| 难题 POC | 实施角色 + 批复角色 | 先由实施角色做最小验证，再由批复角色判定 POC 是否通过 |

**此阶段落实文档（按优先级）：** 任务文档 → 实施经验文档

### 第四阶段：总结报告

**所有调度任务实施完成并验收通过后**，主 Agent 生成总结报告：

**报告模板：**

```markdown
## 📋 实施总结报告

### 任务概览

| 任务标题 | 解决状态 | 实施角色 | 验收问题 | 实施错误 | 技术方案 | 使用工具 |
|----------|----------|----------|----------|----------|----------|----------|
| | ✅/❌/⚠️ | | | | | MCP: / Skill: |

### 问题记录

| 问题类型 | 问题描述 | 出现阶段 | 解决方案 | 关联任务 |
|----------|----------|----------|----------|----------|
| 理解偏差 / 实施错误 / 验收问题 | | 需求/实施/验收 | | |

### 方案详情

| 技术方案 | 适用任务 | 选择理由 | 替代方案 |
|----------|----------|----------|----------|
| | | | |

### 工具使用

| 工具类型 | 工具名称 | 使用场景 | 效果 |
|----------|----------|----------|------|
| MCP | | | |
| Skill | | | |

### 文档更新

| 文档 | 更新内容 |
|------|----------|
| 需求文档 | |
| 进度文档 | |
| 任务文档 | |
| 实施经验文档 | |
```

**此阶段落实文档（按优先级）：** 任务文档 → 进度文档 → 实施经验文档 → 需求文档

---

## 三、记忆体系

| 记忆类型 | 优先级 | 存储位置 | 更新时机 | 保留策略 | 具体流转判断标准 |
|----------|--------|----------|----------|----------|-----------------|
| 需求记忆 | 🔴 高度 | 需求文档 + FACT.md | 第一阶段确认后 | 项目生命周期内持久保留 | 需求文档每条需求有编号和状态；FACT.md 中提取关键词和目标 |
| 进度记忆 | 🔴 高度 | 进度文档 | 每阶段推进时 | 项目生命周期内持续维护 | 进度百分比 = 已完成任务数 / 总任务数 × 100%；状态变更必须更新时间戳 |
| 任务记忆 | 🔴 高度 | 任务文档 | 第二阶段确认 + 第三阶段更新 | 项目生命周期内持续维护 | 任务状态变更必须记录开始/结束时间；是否解决列必须与实际验收结果一致 |
| 纠错记忆 | 🟠 中度 | 实施经验文档 | 第三阶段验收 + 第四阶段 | 长期保留，反复出现的问题提升至 FACT.md | 提升标准：实施经验文档中同一问题编号出现≥3次，或不同编号但问题描述本质相同累计≥3次 |
| 报错记忆 | 🟠 中度 | 实施经验文档 | 实施过程 + 验收过程 | 定期归档，高频问题提升至纠错记忆 | 提升标准：同一报错信息出现≥2次，从"报错记录"区域移至"反复出现的问题"区域 |

**记忆流转规则（具体判断标准）：**
- **提升条件**：实施经验文档中，问题描述本质相同的条目累计出现≥3次 → 复制到 FACT.md，标注来源编号和首次出现时间
- **降级条件**：FACT.md 中某条目超过6个月未被任何任务/进度/需求文档引用（搜索所有项目文档无匹配） → 移回实施经验文档归档区
- **会话初始化**：每次会话开始时，主 Agent 必须执行以下读取，确保上下文连续：
  1. 读取需求文档 → 获取当前需求列表和状态
  2. 读取进度文档 → 获取当前进度百分比和状态
  3. 读取 FACT.md → 获取持久知识
  4. 读取实施经验文档中的"反复出现的问题"区域 → 避免重复犯错

---

## 四、工具体系

### MCP 工具

| MCP 工具 | 用途 | 适用阶段 | 调用角色 |
|----------|------|----------|----------|
| 文件系统 MCP | 读写项目文件 | 实施阶段 | 实施角色、文档维护角色 |
| 数据库 MCP | 数据库操作、查询验证 | 实施阶段 | 实施角色、测试角色 |
| Git MCP | 版本控制、分支管理 | 实施阶段 | 实施角色 |
| 浏览器 MCP | 页面预览、UI 验证 | 验收阶段 | 测试角色、内容审查角色 |
| API 调用 MCP | 接口调试、第三方集成 | 实施阶段 | 实施角色、测试角色 |
| 搜索 MCP | 技术文档检索、方案调研 | 所有阶段 | 所有角色 |
| 终端 MCP | 执行构建/部署/测试命令 | 实施阶段 | 实施角色、测试角色 |

### Skills 技能

| Skill | 用途 | 触发场景 |
|-------|------|----------|
| pua-skill | 批复审视 | 每个 loop 环节自动引用 |
| 代码规范 skill | 强制执行代码风格 | 实施角色编写代码时 |
| 安全检查 skill | 安全漏洞扫描 | 内容审查角色验收时 |
| 测试生成 skill | 自动生成测试用例 | 测试角色编写测试时 |
| 文档规范 skill | 文档格式校验 | 文档维护角色写入文档时 |
| 性能优化 skill | 性能问题检测 | 内容审查角色验收时 |
| 重构建议 skill | 代码可维护性改善 | 实施角色重构时 |

> 具体 Skill 选择根据项目技术栈动态配置，以上为通用推荐。

---

## 五、输出规范

### 中文优先原则

**所有产出物必须使用中文**，具体规则：

| 产出类型 | 具体规则 | 不通过条件 |
|----------|---------|-----------|
| 文档内容 | 需求文档、进度文档、任务文档、实施经验文档的所有文字内容使用中文 | 文档中出现非必要的英文段落（技术术语除外） |
| 沟通内容 | 主 Agent 与用户的对话、各角色之间的沟通使用中文 | 与用户沟通时使用英文回复 |
| 代码注释 | 所有行内注释、块注释、文档注释使用中文 | 注释为英文（`// 获取用户信息` ✅ / `// get user info` ❌） |
| 提交信息 | Git commit message 使用中文 | commit message 为英文 |
| 测试描述 | 测试用例的描述文字使用中文 | 测试用例名为英文描述 |
| 报错翻译 | 代码运行时产生的英文报错信息，在向用户报告或写入文档时，**必须附带中文翻译** | 报告中出现原始英文报错但无中文翻译 |

**技术术语处理规则：**
- 通用技术名词保留英文原文：`React`、`API`、`SQL`、`MCP`、`Git`、`npm` 等
- 有公认中文译名时使用中文：`数据库`、`中间件`、`依赖注入`、`接口` 等
- 代码中的变量名、函数名、文件名遵循项目既有命名规范（通常是英文），但**注释和文档用中文解释其含义**

**英文报错翻译格式：**
```
原始报错：TypeError: Cannot read properties of undefined (reading 'map')
中文翻译：类型错误：无法读取未定义变量的"map"属性（可能原因：接口返回数据为空，未做判空处理）
```

### ASCII 可视化规范

在文档编写、方案展示、任务编排中，**必须使用 ASCII 图表进行可视化**，具体要求：

#### 必须使用可视化图表的场景

| 场景 | 推荐图表类型 | 说明 |
|------|-------------|------|
| 方案流程说明 | 流程图 | 展示步骤间的执行顺序、判断分支、loop 回路 |
| 任务体系呈现 | WBS 工作分解结构 | 展示任务层级、父子关系、任务总量 |
| 多角色协作过程 | 泳道图 | 展示各角色在时间线上的职责区间和交接点 |
| 系统架构 | 架构图 | 展示模块关系、数据流向、依赖层次 |
| 目录结构 | 树形图 | 展示文件/目录的组织关系 |
| 时序关系 | 时序图 | 展示角色间消息传递的先后顺序 |

#### ASCII 图表示例

**流程图（方案说明必用）：**
```
用户需求 → [需求讨论] → 批复通过？ ─否→ 修正需求 → 重新讨论
                         │是
                         ▼
                    [任务规划] → 批复通过？ ─否→ 修正规划 → 重新规划
                         │是
                         ▼
                    [实施调度] → 验收通过？ ─否→ 修正实施 → 重新验收
                         │是
                         ▼
                    [总结报告]
```

**WBS 工作分解结构（任务体系必用）：**
```
项目总目标
├── 模块A：用户管理
│   ├── A1 用户注册      [P0] [⬜未开始]
│   ├── A2 用户登录      [P0] [⬜未开始]
│   ├── A3 权限管理      [P1] [⬜未开始]
│   └── A4 个人信息修改   [P2] [⬜未开始]
├── 模块B：订单系统
│   ├── B1 订单创建      [P0] [⬜未开始]
│   ├── B2 订单查询      [P0] [⬜未开始]
│   ├── B3 订单状态流转   [P1] [⬜未开始]
│   └── B4 退款处理      [P2] [⬜未开始]
└── 模块C：基础设施
    ├── C1 数据库设计     [P0] [⬜未开始]
    ├── C2 接口鉴权      [P0] [⬜未开始]
    └── C3 日志监控      [P1] [⬜未开始]
```

**泳道图（多角色协作必用）：**
```
角色        │ 阶段1:需求讨论      │ 阶段2:任务规划      │ 阶段3:实施调度           │ 阶段4:总结
────────────┼────────────────────┼────────────────────┼─────────────────────────┼──────────
用户        │ ●提出需求           │                    │                         │ ●接收报告
            │ ●确认需求           │                    │                         │
主Agent     │ ●理解需求           │ ●分解任务           │ ●分派任务               │ ●生成报告
            │ ●修正理解           │ ●修正规划           │ ●验收结果               │
批复角色    │ ◆批复需求           │ ◆批复规划           │ ◆批复审查结论           │
            │                    │                    │ ◆批复测试结论           │
实施角色    │                    │                    │ ▶实施任务               │
文档维护    │    ◇写需求文档      │    ◇写进度+任务文档 │          ◇更新任务+经验文档│
内容审查    │                    │                    │          △审查实施内容   │
测试角色    │                    │                    │          △测试功能+业务链 │

图例：●用户操作  ◆批复  ◇文档  ▶实施  △验收
```

**架构图：**
```
┌─────────────┐     ┌─────────────┐
│   用户(我)   │────▶│  主 Agent   │
└─────────────┘     │  (Leader)   │
                    └──────┬──────┘
                           │ 调度
            ┌──────────────┼──────────────┐
            ▼              ▼              ▼
     ┌────────────┐ ┌────────────┐ ┌────────────┐
     │  实施角色   │ │  文档维护   │ │  批复角色   │
     │  (Worker)  │ │ (DocKeeper)│ │ (Approver) │
     └─────┬──────┘ └────────────┘ └─────┬──────┘
           │ 完成后                      │ 批复
           ▼                             │
     ┌──────────┐  ┌──────────┐          │
     │ 内容审查  │  │  测试角色  │          │
     │(Reviewer)│  │ (Tester) │          │
     └─────┬────┘  └────┬─────┘          │
           │            │                │
           └────────────┴────────────────┘
                  验收结论交由批复角色批复
```

**目录结构树形图：**
```
project/
├── .project/
│   ├── requirement.md     ← 需求文档
│   ├── progress.md        ← 进度文档
│   ├── task.md            ← 任务文档
│   └── learned.md         ← 实施经验文档
├── src/
│   ├── modules/
│   └── utils/
└── tests/
```

### 表格输出

所有汇报、文档、对比分析优先使用表格格式，提升可读性。

---

## 六、协作方式

### 可视化优先原则

在方案描述、任务编排、进度汇报等场景中，**必须优先使用 ASCII 可视化图表**：

| 场景 | 必须使用的可视化方式 | 具体要求 |
|------|---------------------|----------|
| 方案流程 | ASCII 流程图 | 必须包含判断分支、loop 回路、异常路径 |
| 任务体系 | ASCII WBS 工作分解结构 | 必须展示任务层级、优先级、状态；≥⭐⭐⭐的难题任务用 `⚠️` 标记 |
| 多角色协作 | ASCII 泳道图 | 必须展示每个角色在时间线上的活跃区间和交接点 |
| 系统架构 | ASCII 架构图 | 必须展示模块关系和数据流向 |
| 目录结构 | ASCII 树形图 | 必须标注每个文件/目录的用途 |

**可视化检查标准：**

| 检查项 | 通过条件 | 不通过条件 |
|--------|----------|-----------|
| 方案是否有流程图 | 每个方案都附带 ASCII 流程图 | 方案只有文字描述无流程图 |
| 任务是否有 WBS | 任务列表同时提供 WBS 树形图 | 只有表格无 WBS 层级图 |
| 多角色场景是否有泳道图 | 涉及≥2个角色的流程都附带泳道图 | 多角色流程无泳道图 |
| 难题是否在图中标记 | WBS 中难题任务用 `⚠️` 标记，流程图中难题路径用虚线 `┄` 标识 | 难题在图中无任何区分 |

### 工作流编排规范

描述任务时遵循以下格式：

```markdown
## 任务编排

### 步骤 1：[动作]
- 执行者：[角色]
- 输入：[依赖什么]
- 动作：[具体做什么]
- 产出：[输出什么]
- 验收标准：[怎样算完成]

### 步骤 2：[动作]
- 执行者：[角色]
- 输入：[步骤1的产出]
- 动作：[具体做什么]
- 产出：[输出什么]
- 验收标准：[怎样算完成]

...

### 回退条件
- 若 [某条件]，则回退至步骤 N 重新执行
```

### 使用方式

- 所有需求描述、任务描述使用**规范格式**，越具体越好
- 避免模糊表述，必须包含：**做什么、为什么做、怎么做、做完的标准**
- 主 Agent 在调度时必须将任务描述完整传递给对应角色，不允许省略上下文

---

## 七、实施路线

将本方案落地为可用的 Claude Skill / Agent 配置，分以下步骤：

### Step 1：创建 pua-skill 的集成
- 安装并配置 [pua-skill](https://github.com/tanweai/pua/blob/main/README.zh-CN.md)
- 确保批复角色可以正确调用

### Step 2：创建文档模板 Skill
- 封装需求文档、进度文档、任务文档、实施经验文档的模板
- 文档维护角色调用时自动应用模板

### Step 3：创建四阶段工作流 Skill
- 封装四个阶段的流程逻辑
- 包含 loop 机制、批复调度、验收流程

### Step 4：创建角色配置
- 定义各角色的职责边界、调用权限、输入输出规范
- 确保实施角色不能调用其他角色

### Step 5：创建记忆管理 Skill
- 自动化文档读写和记忆流转规则
- 高频问题自动提升至 FACT.md

### Step 6：整合测试
- 用一个实际小项目验证完整流程
- 根据运行结果调整优化

---

> 本方案持续维护在 `make.md`，与 `my.md` 原始设计对照更新。
